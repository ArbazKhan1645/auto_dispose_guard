name: auto_dispose_guard_example
description: Example app demonstrating the auto_dispose_guard package.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  auto_dispose_guard:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
